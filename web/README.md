# impostarr web

`npm install && npm run dev` — dev server on `:5173`, proxies `/api` to `http://localhost:8000` (run the backend separately). `npm run build` for a production build, `npm test` for vitest.
