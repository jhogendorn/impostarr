# Copy to compose.env and fill in. Referenced by docker/compose.yml
# (compose.env is read for interpolation automatically by `docker compose`
# when named `.env`; rename or pass --env-file=docker/compose.env if kept
# as compose.env instead).

# Host path to your media library, bind-mounted read-only at /media.
MEDIA_PATH=/path/to/media

# Optional: host path for the trash retention mount (/trash). Must be on
# the SAME filesystem as MEDIA_PATH for cheap hardlinks (see
# examples/impostarr.yml's trash section) — falls back to a full copy
# otherwise. Defaults to ./trash next to this compose file if unset.
TRASH_PATH=/path/to/media/.impostarr-trash

# Optional: uid/gid the container process runs as (see README PUID/PGID
# section). Must match the ownership of ./config, ./assets, ./models on the
# host. Defaults to 1000:1000 if unset.
PUID=1000
PGID=1000

# Optional: only needed with `docker compose --profile postgres up`.
POSTGRES_USER=impostarr
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=impostarr
